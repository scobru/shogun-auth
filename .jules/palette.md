## 2024-05-22 - Missing Labels in Forms
**Learning:** The application uses placeholders as labels in forms (e.g., EncryptedDataManager), which is an accessibility anti-pattern.
**Action:** Always check for label elements when encountering form inputs and add visible labels with proper associations.
